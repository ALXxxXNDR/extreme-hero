class_name DealCardLibrary
extends RefCounted

# =============================================================================
# V2 — 스킬 카드 데이터 (v3 재저작 · 28장 = 7원소 × 4장)
# =============================================================================
# 기준: docs/GAME_DESIGN_V3.md §4.2(원소 7계) · §5.1~5.4(28장·id 고정·개명) ·
#       부록 A-2 ⑮⑱⑲ · 부록 B V2 / v2 기반은 GAME_DESIGN_V2.md §3.8 · 부록 C-6
#
# v2 → v3에서 달라진 것은 **태그와 이름뿐이다. id는 한 개도 바뀌지 않았다.**
#
#   ① **legacy 8장을 전부 드래프트 풀로 승격했다 → 정확히 28장.**
#      v2는 드래프트 20 + legacy 8이었고, legacy는 `draft_pool()`에서만 빠져 있었을 뿐
#      by_id·아이콘·VFX 분기가 전부 살아 있었다. `"legacy": true` 키를 지웠다.
#      `is_legacy()`는 항상 false를 돌려주지만 **함수는 남겼다**(호출부 보호).
#
#   ② **원소 7계로 재배치**(§4.2 · §5.3). blood→poison / light→psi / iron→strike는
#      개명이 아니라 **역할 승계**이고, `oil`만 신규다. 그래서 28장의 태그를 처음부터
#      다시 붙일 필요가 없었다 — 이번 개편에서 구현 규모를 가장 크게 줄인 판단이다.
#
#   ③ **id를 하나도 바꾸지 않는다.** id 하나당 동기화 지점이 10곳이다
#      (`skill_icon.GENERATED_SKILL_INDEX` · `cycle_skill_effect._draw_signature` ·
#       `player._apply_skill_stats` · `palette.gd` · `patch_library` · `debt_module()` ·
#       `data_test` · `test_runner` 다수 · 아틀라스 PNG 자체).
#      id 고정 = 그 10곳 전부 무변경. 아이콘 아틀라스 7×4 = 28칸도 그대로 맞는다.
#
#   ④ **표시명 개명은 원소가 바뀐 카드에만 했다.** 스킬 표시명은 W7이 이미 판타지로
#      통일해 뒀고(§5.4), 남은 직장 풍자는 **id에만** 있으며 id는 ③ 때문에 못 바꾼다.
#      진짜 풍자 개명 대상은 `item_library.gd`다(같은 웨이브가 처리했다).
#
#   ⑤ `reload` 재기준(v2 부록 C-6)은 그대로 승계한다. 실측(test/data_test.gd,
#      대표 5칸 덱 5종 × 400사이클) 목표는 여전히 "평균 ≈3초 / 상한 6초"다.
#
# 기존 키(kind/damage/duration/action_ratio/reload/range/arc/hits/...)는 하나도
# 지우지 않았다. v1 딜싸이클 컨트롤러가 아직 그 키들을 읽는다.
#
# ── X1(2026-08-09) `desc` 40장 전수 점검 ─────────────────────────────────────
# 사용자 피드백 ③: "'3회 타격' 같은 태그는 모두 지우고, 타격 횟수·넉백 같은 건
# 스킬 설명 문장에 포함시켜." 레벨업 모달이 태그 줄(`combat_tags()`)을 더 이상
# 그리지 않으므로 그 줄이 나르던 정보를 **문장이 흡수해야** 한다.
#   * 손댄 것은 `desc` **문자열뿐**이다. id·name·수치·태그는 한 글자도 안 바뀌었다.
#   * 28장 중 19장은 이미 "여섯 차례" "세 번" 식으로 횟수를 품고 있어 **그대로 뒀다**
#     (`rapid_slash` "짧은 검격을 세 번 빠르게 이어…"가 사용자가 직접 예시로 든 문장이다).
#   * 고친 9장은 빠진 축 하나씩을 문장에 넣었다 — 횟수(`aura`), 넉백(`cleave`
#     `dash_blade` `meteor_blade`), 사거리 성격(`earth_splitter` `thunder`
#     `targeting` `recursion` `blade_fan`).
#   * SPECIALS 12장(트로피 카드)도 같은 기준으로 횟수·수호막을 문장에 넣었다.
#   * `combat_tags()` 함수는 **지우지 않았다** — 편집 화면·상점 등 다른 화면이
#     아직 쓴다. 그 화면들의 정리는 X2 몫이다.
#
# ── V2가 손댄 수치 2건 (설계가 강제한 것) ────────────────────────────────────
# §5.1의 완료 기준은 "원소마다 근접(range < 200) ≥1 · 원거리(≥260) ≥1"이다.
# 그런데 28장 중 range ≥ 260인 카드는 **다섯 장뿐이다**
# (earth_splitter 620 · boomerang_blade 540 · targeting 520 · blade_fan 510 · recursion 480).
# 5 < 7이므로 §5.3의 배치를 어떻게 섞어도 두 원소는 원거리 카드를 가질 수 없다 —
# **구조적으로 range 상향 2건이 강제된다.** 배치는 §5.3 표를 한 글자도 바꾸지 않고
# 대신 그 두 원소(빙·독)의 광역 카드 사거리를 올리고 그만큼 reload/damage로 지불했다.
#   frost_ring  range 205 → 268 · reload 0.53 → 0.60 · damage 1.05 → 0.95
#   whirlwind   range 148 → 262 · reload 0.46 → 0.58 · damage 0.82 → 0.72
# 최종 확정은 V10 balance_probe. 근거는 docs/handoff-v2.md §2에 남겼다.

# -----------------------------------------------------------------------------
# 태그 어휘 (§4.2) — RuneEngine.ELEMENTS / RuneEngine.FORMS와 반드시 같아야 한다.
# **배열 순서까지 같아야 한다**(data_test가 `Array == Array`로 대조한다).
# 여기서 preload로 묶지 않은 이유: 데이터 파일이 엔진에 의존하면 순환 로드가 생긴다.
# -----------------------------------------------------------------------------
const ELEMENTS: Array[String] = ["fire", "ice", "thunder", "poison", "oil", "strike", "psi"]
const FORMS: Array[String] = ["slash", "pierce", "wave", "trap", "guard"]

# 드래프트 풀 크기(§5.1 "7원소 × 4장"). data_test가 이 값으로 검사한다.
# v2의 20에서 28로 올랐다 — legacy 8장 승격분이다. 신규 id는 0개다.
const DRAFT_POOL_SIZE := 28
## 원소 하나가 가져야 하는 카드 수. 28 = 7 × 4의 4다.
const CARDS_PER_ELEMENT := 4
## 근접/원거리 판정선(§5.1). 이 사이(200~260)는 어느 쪽으로도 세지 않는다.
const MELEE_RANGE_MAX := 200.0
const RANGED_RANGE_MIN := 260.0
# 랭크 상한(§5.2 "상한 R3"). 같은 id 4장 = R3.
const MAX_RANK := 3
# 랭크 계수(§9.3): 피해 ×(1+0.55(r−1)) · duration ×0.94^(r−1) · reload ×0.95^(r−1)
const RANK_DAMAGE_STEP := 0.55
const RANK_DURATION_FALLOFF := 0.94
const RANK_RELOAD_FALLOFF := 0.95
# 랭크가 범위를 넓히는 폭(v1 0.055 유지 — §9.3이 언급하지 않은 축이라 건드리지 않았다).
const RANK_RANGE_STEP := 0.055

# 빈 칸이 실행하는 기본 공격.
# **태그가 없다(빈 문자열).** 의도적이다 — 빈 칸까지 원소를 가지면 카드를 한 장도
# 안 얻은 1일차에 5칸 결속 + 삼각 배열이 공짜로 성립한다. RuneEngine.BASIC_CARD도
# 같은 이유로 태그가 비어 있고, `bond_mask`/`triangle_ok`는 빈 태그를 건너뛴다.
const BASIC: Dictionary = {
	"id":"basic", "name":"기본 베기", "symbol":"/", "kind":"melee",
	"element":"", "form":"",
	"desc":"빈 딜싸이클 칸이 실행하는 검사의 기본 공격입니다.",
	"damage":1.0, "duration":0.72, "action_ratio":0.78, "reload":0.11,
	"range":132.0, "arc":2.18, "hits":1, "projectiles":1,
	"color":"f4d35e", "debt_desc":"마왕의 기본 공격 회전이 빨라집니다."
}

# -----------------------------------------------------------------------------
# SKILLS = 드래프트 풀 28장 = **7원소 × 4장** (§5.1 · §5.3). legacy 0장.
# 배열은 원소 블록 7개로 묶여 있고 블록 순서 = ELEMENTS 순서다.
#
# 원소 분포: 화4 · 빙4 · 뇌4 · 독4 · 유4 · 타4 · 초4   → 결속(같은 원소 3칸 연속)이
#            **7원소 전부에서** 가능하다.
# 형태 분포: 참격7 · 관통6 · 파동5 · 설치5 · 수호5      → 삼각(1·3·5 같은 형태)이
#            **5형태 전부에서** 가능하다(형태당 ≥5).
# 근접/원거리: 원소마다 range < 200 이 최소 1장, range ≥ 260 이 최소 1장.
#
# ── v2에서 형태를 옮긴 카드 2장 (형태당 ≥5를 맞추기 위한 최소 이동) ──────────
#   `aura`         파동 → **설치**  기름은 바닥에 눌어붙는다. 9틱 지속 장판이라
#                                   원래도 설치에 가까웠다(§5.3 "oil 4장은 설치·파동·투척").
#   `phantom_step` 참격 → **수호**  세 잔상이 대신 맞아 준다. 돌진 회피기의 재해석.
#   이 둘을 고른 이유: 설계 §4.6의 실증 빌드 3종이 지목한 카드
#   (frost_ring·moon_barrier·thunder·time_cut·targeting / cross_cut·whirlwind·
#    blood_pact·execution·flame_field / gravity_well·sword_rain·meteor_blade·
#    earth_splitter)의 형태를 **하나도 건드리지 않는 유일한 조합**이다.
# -----------------------------------------------------------------------------
const SKILLS: Array[Dictionary] = [
	# --- 화(火) · 설치와 폭발 · 모든 폭발의 점화원 -------------------------------
	{"id":"flame_field","name":"업화의 장막","symbol":"F","kind":"ground","element":"fire","form":"trap","desc":"현재 위치에 여섯 차례 타오르는 화염 장판을 남깁니다.","damage":0.72,"duration":1.60,"action_ratio":0.86,"reload":0.60,"range":112.0,"arc":6.283,"hits":6,"color":"e78a45","debt_desc":"마왕의 전장이 불타기 시작합니다."},
	{"id":"meteor_blade","name":"운석검 강림","symbol":"*","kind":"ground","element":"fire","form":"trap","desc":"세 자루의 운석검이 주변 여러 곳에 차례로 떨어져 크게 밀쳐냅니다. 끝난 뒤 긴 정지가 남습니다.","damage":2.45,"duration":1.95,"action_ratio":0.70,"reload":0.98,"range":235.0,"arc":6.283,"hits":3,"random_impacts":true,"heavy":true,"color":"d95763","debt_desc":"마왕이 전장을 뒤엎는 운석을 얻습니다."},
	{"id":"earth_splitter","name":"용암 균열","symbol":"E","kind":"projectile","element":"fire","form":"pierce","desc":"느리고 굵은 용암 충격파 두 줄이 정면 아주 멀리까지 적을 꿰뚫고 밀어냅니다.","damage":1.62,"duration":1.32,"action_ratio":0.65,"reload":0.62,"range":620.0,"arc":0.18,"hits":2,"projectiles":1,"pierce":5,"heavy":true,"color":"e78a45","debt_desc":"마왕이 관통 충격파를 얻습니다."},
	{"id":"lion_roar","name":"업염 사자후","symbol":"W","kind":"area","element":"fire","form":"wave","desc":"두 겹의 불길 충격파가 주변 적을 멀리 밀어냅니다.","damage":1.05,"duration":1.18,"action_ratio":0.78,"reload":0.42,"range":215.0,"arc":6.283,"hits":2,"knockback":62.0,"heavy":true,"color":"e78a45","debt_desc":"마왕의 포효가 접근을 봉쇄합니다."},
	# --- 빙(氷) · 감속과 전도의 매개 --------------------------------------------
	{"id":"dash_blade","name":"설한 섬격","symbol":">","kind":"dash","element":"ice","form":"slash","desc":"서릿발을 끌며 돌진해 경로를 두 번 베고 부딪힌 적을 세게 밀쳐냅니다.","damage":1.02,"duration":0.86,"action_ratio":0.90,"reload":0.36,"range":185.0,"arc":0.62,"hits":2,"color":"67c7d4","debt_desc":"마왕이 돌진 공격을 배웁니다."},
	{"id":"frost_ring","name":"빙결 파문","symbol":"Q","kind":"area","element":"ice","form":"wave","desc":"세 겹의 냉기 파동이 아주 넓은 범위를 얼려 붙입니다.","damage":0.95,"duration":1.28,"action_ratio":0.68,"reload":0.60,"range":268.0,"arc":6.283,"hits":3,"slow":0.32,"color":"67c7d4","debt_desc":"마왕의 광역 파동이 강화됩니다."},
	{"id":"guardian_blade","name":"빙정 호위검","symbol":"G","kind":"orbit","element":"ice","form":"guard","desc":"여덟 차례 회전하는 호위검이 몸 주변을 지켜냅니다.","damage":0.54,"duration":1.65,"action_ratio":0.96,"reload":0.48,"range":102.0,"arc":6.283,"hits":8,"color":"83c65c","debt_desc":"마왕 주위에 검은 칼날이 돕니다."},
	{"id":"moon_barrier","name":"빙정 결계","symbol":"M","kind":"shield","element":"ice","form":"guard","desc":"공격 대신 얼음 수호막 두 겹을 즉시 확보합니다.","damage":0.0,"duration":0.94,"action_ratio":0.48,"reload":0.50,"range":0.0,"arc":0.0,"hits":1,"shield":2,"color":"67c7d4","debt_desc":"마왕의 검은 갑주가 두꺼워집니다."},
	# --- 뇌(雷) · 전이와 광역화 --------------------------------------------------
	{"id":"thunder","name":"뇌전 심판","symbol":"T","kind":"chain","element":"thunder","form":"wave","desc":"멀리 있는 적까지 자동으로 찾아 네 번 연쇄되는 벼락입니다.","damage":1.02,"duration":1.15,"action_ratio":0.90,"reload":0.50,"range":410.0,"arc":6.283,"hits":4,"ricochet":4,"color":"67c7d4","debt_desc":"마왕의 추적 공격이 집요해집니다."},
	{"id":"time_cut","name":"섬전 순참","symbol":"0","kind":"melee","element":"thunder","form":"slash","desc":"눈으로 좇을 수 없는 교차 베기 두 번으로 사이클을 이어갑니다.","damage":0.58,"duration":0.82,"action_ratio":0.96,"reload":0.12,"range":116.0,"arc":1.7,"hits":2,"color":"83c65c","debt_desc":"마왕의 사이클이 더 빨라집니다."},
	{"id":"targeting","name":"뇌창 유도","symbol":"+","kind":"projectile","element":"thunder","form":"pierce","desc":"적을 스스로 좇는 뇌창을 두 차례 아주 먼 곳까지 쏘아 보냅니다.","damage":0.92,"duration":0.84,"action_ratio":0.84,"reload":0.26,"range":520.0,"arc":0.2,"hits":2,"projectiles":1,"pierce":1,"homing":1.8,"color":"f4d35e","debt_desc":"마왕의 투사체가 플레이어를 추적합니다."},
	{"id":"phantom_step","name":"뇌영 잔상진","symbol":">>","kind":"dash","element":"thunder","form":"guard","desc":"세 개의 뇌광 잔상이 대신 맞아 주며 돌진 경로를 벱니다.","damage":0.82,"duration":1.02,"action_ratio":0.96,"reload":0.38,"range":205.0,"arc":0.78,"hits":3,"color":"67c7d4","debt_desc":"마왕이 잔상과 함께 돌진합니다."},
	# --- 독(毒) · 축적하고 터뜨린다 ---------------------------------------------
	{"id":"whirlwind","name":"역병 회전참","symbol":"O","kind":"area","element":"poison","form":"wave","desc":"역병의 바람을 두 바퀴 두르고 아주 넓게 네 차례 피해를 줍니다.","damage":0.72,"duration":1.30,"action_ratio":0.94,"reload":0.58,"range":262.0,"arc":6.283,"hits":4,"color":"d95763","debt_desc":"마왕이 전방위 난무를 익힙니다."},
	{"id":"blood_pact","name":"역병의 서약","symbol":"B","kind":"melee","element":"poison","form":"slash","desc":"세 번 베고 스며든 독기 일부를 체력으로 돌려받습니다.","damage":0.92,"duration":1.05,"action_ratio":0.90,"reload":0.38,"range":142.0,"arc":1.92,"hits":3,"lifesteal":0.08,"color":"d95763","debt_desc":"마왕의 재생력이 증가합니다."},
	{"id":"execution","name":"독월 처형","symbol":"X","kind":"melee","element":"poison","form":"slash","desc":"한 번에 큰 피해를 주지만 검을 회수하는 시간이 깁니다.","damage":4.20,"duration":1.62,"action_ratio":0.55,"reload":0.77,"range":158.0,"arc":1.55,"hits":1,"heavy":true,"color":"d95763","debt_desc":"마왕의 단발 화력이 폭증합니다."},
	{"id":"cross_cut","name":"맹독 십자참","symbol":"#","kind":"melee","element":"poison","form":"slash","desc":"서로 반대 방향의 맹독 검격 두 줄을 교차시킵니다.","damage":1.18,"duration":0.94,"action_ratio":0.92,"reload":0.28,"range":165.0,"arc":2.15,"hits":2,"color":"fff3d0","debt_desc":"마왕의 검격이 교차하며 빈틈을 지웁니다."},
	# --- 유(油) · 피해 0. 먼저 칠하고 나중에 태운다 -----------------------------
	# 4장이 전부 설치·투척인 것은 의도다(§5.3). 기름은 바닥에 칠하는 원소여야
	# "먼저 칠하고 나중에 태운다"는 **순서**가 자연스러워진다.
	{"id":"gravity_well","name":"기름 늪","symbol":"@","kind":"ground","element":"oil","form":"trap","desc":"끈적한 기름 늪이 적을 끌어당기며 일곱 번 짓누릅니다.","damage":0.52,"duration":1.70,"action_ratio":0.94,"reload":0.67,"range":175.0,"arc":6.283,"hits":7,"pull":0.24,"color":"bd6ac8","debt_desc":"마왕이 도망갈 공간까지 압축합니다."},
	{"id":"sword_rain","name":"역청 폭우","symbol":"|||","kind":"ground","element":"oil","form":"trap","desc":"주변 여러 지점에 일곱 줄기의 역청을 차례로 쏟아붓습니다.","damage":0.88,"duration":2.05,"action_ratio":0.96,"reload":0.73,"range":145.0,"arc":6.283,"hits":7,"random_impacts":true,"color":"67c7d4","debt_desc":"마왕의 검이 하늘에서 끝없이 내려옵니다."},
	{"id":"aura","name":"역청 안개","symbol":"A","kind":"area","element":"oil","form":"trap","desc":"움직이는 동안에도 따라붙는 기름 안개가 아홉 번에 걸쳐 주변을 적십니다.","damage":0.45,"duration":1.90,"action_ratio":0.98,"reload":0.37,"range":136.0,"arc":6.283,"hits":9,"color":"bd6ac8","debt_desc":"마왕의 근접 압박이 강해집니다."},
	{"id":"boomerang_blade","name":"역청 회귀검","symbol":"U","kind":"projectile","element":"oil","form":"pierce","desc":"기름을 묻히며 튕겨 다닌 뒤 다시 날아오는 검기를 두 번 던집니다.","damage":0.88,"duration":1.22,"action_ratio":0.94,"reload":0.43,"range":540.0,"arc":0.3,"hits":2,"projectiles":1,"ricochet":4,"color":"f4d35e","debt_desc":"마왕의 검기가 기름을 묻히며 돌아옵니다."},
	# --- 타(打) · 비원소. 상태를 **터뜨린다** -----------------------------------
	{"id":"cleave","name":"반월참","symbol":"C","kind":"melee","element":"strike","form":"slash","desc":"한 번의 넓은 반달 궤적으로 전방을 묵직하게 쓸어 적을 밀쳐냅니다.","damage":1.65,"duration":0.98,"action_ratio":0.72,"reload":0.31,"range":176.0,"arc":2.72,"hits":1,"color":"f4d35e","debt_desc":"마왕의 검격 범위와 위력이 증가합니다."},
	{"id":"rapid_slash","name":"삼연참","symbol":"3","kind":"melee","element":"strike","form":"slash","desc":"짧은 검격을 세 번 빠르게 이어 빈틈을 줄입니다.","damage":0.62,"duration":1.05,"action_ratio":0.95,"reload":0.26,"range":126.0,"arc":1.74,"hits":3,"color":"67c7d4","debt_desc":"마왕이 연속 공격을 배웁니다."},
	{"id":"thrust","name":"관통 창격","symbol":"!","kind":"melee","element":"strike","form":"pierce","desc":"좁고 긴 찌르기를 두 번 반복해 정면을 관통합니다.","damage":1.28,"duration":0.88,"action_ratio":0.88,"reload":0.29,"range":228.0,"arc":0.48,"hits":2,"color":"fff3d0","debt_desc":"마왕의 찌르기가 더 멀리 닿습니다."},
	{"id":"recursion","name":"회귀 검기","symbol":"R","kind":"projectile","element":"strike","form":"pierce","desc":"멀리 나아가 적 사이를 튕기며 두 차례 되돌아오는 검기입니다.","damage":0.90,"duration":1.10,"action_ratio":0.92,"reload":0.46,"range":480.0,"arc":0.24,"hits":2,"projectiles":1,"ricochet":3,"color":"bd6ac8","debt_desc":"마왕의 공격이 여러 번 되돌아옵니다."},
	# --- 초(超) · 비원소. 상태를 **환금한다** -----------------------------------
	{"id":"shield_bash","name":"염동 방패치기","symbol":"D","kind":"melee","element":"psi","form":"guard","desc":"염력으로 방패를 두 번 밀어치고 수호막 한 겹을 얻습니다.","damage":0.82,"duration":0.90,"action_ratio":0.78,"reload":0.36,"range":122.0,"arc":2.28,"hits":2,"shield":1,"knockback":28.0,"color":"67c7d4","debt_desc":"마왕이 방어와 공격을 동시에 챙깁니다."},
	{"id":"holy_pulse","name":"초념 파동","symbol":"H","kind":"area","element":"psi","form":"wave","desc":"넓은 정신 파동을 두 번 폭발시키고 잠시 숨을 고릅니다.","damage":1.55,"duration":1.40,"action_ratio":0.56,"reload":0.65,"range":245.0,"arc":6.283,"hits":2,"heavy":true,"color":"fff3d0","debt_desc":"마왕의 광역 공격이 정신을 갉아냅니다."},
	{"id":"battle_trance","name":"초월 명상","symbol":"I","kind":"area","element":"psi","form":"guard","desc":"세 번의 정신 파동과 함께 체력과 수호막을 조금 회복합니다.","damage":0.42,"duration":1.18,"action_ratio":0.88,"reload":0.46,"range":125.0,"arc":6.283,"hits":3,"heal":2.0,"shield":1,"color":"83c65c","debt_desc":"마왕이 싸울수록 회복하는 법을 익힙니다."},
	{"id":"blade_fan","name":"염동 검선","symbol":"V","kind":"projectile","element":"psi","form":"pierce","desc":"정면 부채꼴로 다섯 줄기의 염동 검선을 먼 곳까지 한꺼번에 보냅니다.","damage":0.64,"duration":1.00,"action_ratio":0.72,"reload":0.41,"range":510.0,"arc":0.9,"hits":1,"projectiles":5,"spread":0.22,"pierce":1,"color":"83c65c","debt_desc":"마왕의 투사체가 부채꼴로 증식합니다."}
]

# -----------------------------------------------------------------------------
# 특별 스킬 12종 = **보스 트로피 카드** (§5.5 · 부록 A-2 ⑳)
# -----------------------------------------------------------------------------
# v2에서는 계보 3종(성기사/광전사/창기사)의 각성 보상이었다. v3는 계보를 폐기하고
# 이 12장을 **보스 격파 2택1 트로피**로 재해석한다. 손댄 것은 세 가지뿐이다:
#   ① 원소를 역할 승계로 옮겼다(light→psi / blood→poison / iron→strike).
#   ② **계보 색을 지웠다** — 이름과 설명에서 "성기사 전용" 같은 계보 문구를 전부 제거.
#   ③ `tier2` 키는 남긴다. 계보 2차 각성 표식이 아니라 **트로피 등급**으로 재해석되며
#      `trophy_library.gd`가 스테이지 배분에 그대로 읽는다.
# 수치·id·form은 한 글자도 바꾸지 않았다. 배분표는 `trophy_library.gd`가 소유한다.
#
# 특별 카드는 data_test의 "원소당 정확히 4장" 집계에서 제외된다(드래프트 풀이
# 아니기 때문). 다만 원소·형태 어휘 검사는 일반 카드와 똑같이 받는다.
# -----------------------------------------------------------------------------
const SPECIALS: Array[Dictionary] = [
	{"id":"holy_verdict","name":"초념 연속 심판","symbol":"P1","kind":"melee","element":"psi","form":"slash","desc":"넓은 정신의 검을 세 번 휘두르며 수호막 한 겹을 함께 얻습니다.","damage":2.1,"duration":1.35,"action_ratio":0.94,"reload":0.43,"range":198.0,"arc":2.7,"hits":3,"shield":1,"color":"f4d35e","special":true,"debt_desc":"마왕이 타락한 성광검을 배웁니다."},
	{"id":"aegis_process","name":"영겁의 방패 파동","symbol":"P2","kind":"area","element":"psi","form":"guard","desc":"다섯 번의 피해 파동과 수호막 두 겹을 동시에 만듭니다.","damage":1.15,"duration":1.18,"action_ratio":0.9,"reload":0.41,"range":184.0,"arc":6.283,"hits":5,"shield":2,"color":"67c7d4","special":true,"debt_desc":"마왕의 갑주가 공격하기 시작합니다."},
	{"id":"heavens_gate","name":"천상문 개방","symbol":"PX","kind":"area","element":"psi","form":"wave","desc":"화면을 덮는 초념 연타 다섯 번과 수호막 세 겹을 한꺼번에 엽니다.","damage":3.4,"duration":1.15,"action_ratio":0.96,"reload":0.46,"range":330.0,"arc":6.283,"hits":5,"shield":3,"color":"fff3d0","special":true,"tier2":true,"debt_desc":"마왕이 검은 천국문을 엽니다."},
	{"id":"zero_damage_oath","name":"불가침의 서약","symbol":"PZ","kind":"shield","element":"psi","form":"guard","desc":"다섯 겹의 막을 두르고 세 번의 초념 반격을 돌려줍니다.","damage":2.2,"duration":1.25,"action_ratio":0.92,"reload":0.48,"range":260.0,"arc":6.283,"hits":3,"shield":5,"color":"f4d35e","special":true,"tier2":true,"debt_desc":"마왕이 무너지지 않는 갑주를 얻습니다."},
	{"id":"crimson_loop","name":"역병의 난무","symbol":"B1","kind":"melee","element":"poison","form":"slash","desc":"빠를수록 더 많이 베는 역병의 난무를 여섯 번 이어 갑니다.","damage":1.05,"duration":1.32,"action_ratio":0.98,"reload":0.24,"range":155.0,"arc":2.1,"hits":6,"lifesteal":0.06,"color":"d95763","special":true,"debt_desc":"마왕의 난무가 끝나지 않습니다."},
	{"id":"pain_compiler","name":"고통 연성진","symbol":"B2","kind":"area","element":"poison","form":"wave","desc":"주변을 다섯 번 연속 폭발시키며 그만큼 체력을 되찾습니다.","damage":1.3,"duration":1.05,"action_ratio":0.94,"reload":0.43,"range":182.0,"arc":6.283,"hits":5,"lifesteal":0.1,"color":"e78a45","special":true,"debt_desc":"마왕이 고통을 화력으로 바꿉니다."},
	{"id":"red_moon_execution","name":"독월 최후의 참격","symbol":"BX","kind":"melee","element":"poison","form":"slash","desc":"독기 어린 달이 겹치며 여덟 번의 초고속 참격을 쏟아냅니다.","damage":1.75,"duration":1.36,"action_ratio":0.99,"reload":0.22,"range":220.0,"arc":2.55,"hits":8,"lifesteal":0.08,"color":"d95763","special":true,"tier2":true,"debt_desc":"마왕이 적월 난무를 완성합니다."},
	{"id":"immortal_frenzy","name":"불멸의 광란","symbol":"BI","kind":"area","element":"poison","form":"wave","desc":"아홉 번의 광란이 끝날 때까지 죽음조차 그를 데려가지 못합니다.","damage":1.8,"duration":1.2,"action_ratio":1.0,"reload":0.31,"range":245.0,"arc":6.283,"hits":9,"shield":2,"lifesteal":0.12,"color":"d95763","special":true,"tier2":true,"debt_desc":"마왕의 불사 광란이 시작됩니다."},
	{"id":"dragon_pierce","name":"용창 관통격","symbol":"L1","kind":"projectile","element":"strike","form":"pierce","desc":"정면 아주 멀리까지 모든 적을 두 번 꿰뚫는 용의 궤적입니다.","damage":2.8,"duration":1.10,"action_ratio":0.84,"reload":0.38,"range":720.0,"arc":0.16,"hits":2,"projectiles":1,"pierce":9,"color":"67c7d4","special":true,"debt_desc":"마왕이 용의 관통창을 얻습니다."},
	{"id":"echo_thrust","name":"잔영 연속 찌르기","symbol":"L2","kind":"melee","element":"strike","form":"pierce","desc":"같은 찌르기를 다섯 번, 매번 다른 각도로 반복합니다.","damage":1.65,"duration":1.18,"action_ratio":0.97,"reload":0.30,"range":285.0,"arc":0.62,"hits":5,"color":"bd6ac8","special":true,"debt_desc":"마왕의 찌르기가 복제됩니다."},
	{"id":"sky_dragon_array","name":"천룡 진형 폭주","symbol":"LX","kind":"projectile","element":"strike","form":"pierce","desc":"다섯 용창이 화면 끝까지 연속으로 관통합니다.","damage":2.55,"duration":1.42,"action_ratio":1.0,"reload":0.26,"range":850.0,"arc":0.72,"hits":5,"projectiles":3,"pierce":12,"color":"67c7d4","special":true,"tier2":true,"debt_desc":"마왕이 천룡의 진형을 훔칩니다."},
	{"id":"infinite_recursion","name":"영겁 회귀 창술","symbol":"LR","kind":"projectile","element":"strike","form":"pierce","desc":"네 번 튕기고 관통하고 다시 튕기는 전설의 창입니다.","damage":2.15,"duration":1.50,"action_ratio":1.0,"reload":0.23,"range":780.0,"arc":0.35,"hits":4,"projectiles":2,"pierce":6,"ricochet":7,"color":"bd6ac8","special":true,"tier2":true,"debt_desc":"마왕의 창이 멈출 줄을 모릅니다."}
]

# -----------------------------------------------------------------------------
# 조회
# -----------------------------------------------------------------------------

static func all() -> Array[Dictionary]:
	# 28종 전부. v3에서는 draft_pool()과 내용이 같다(legacy 0장).
	return SKILLS.duplicate(true)

static func all_with_specials() -> Array[Dictionary]:
	var result := SKILLS.duplicate(true)
	result.append_array(SPECIALS.duplicate(true))
	return result

## v3 드래프트 풀(§5.1, 28종 = 7원소 × 4장). legacy 승격으로 SKILLS 전량과 같다.
## 레벨업 2택1·성 상점·전조 회수 등 "새 카드를 준다"는 모든 경로는 이 함수를 써야 한다.
## legacy 필터는 **일부러 남겼다** — 앞으로 다시 풀에서 카드를 빼야 할 때
## 데이터 한 키(`"legacy": true`)만 붙이면 되고 호출부는 그대로다.
static func draft_pool() -> Array[Dictionary]:
	var pool: Array[Dictionary] = []
	for card: Dictionary in SKILLS:
		if not bool(card.get("legacy", false)):
			pool.append(card.duplicate(true))
	return pool

static func draft_ids() -> Array[String]:
	var ids: Array[String] = []
	for card: Dictionary in SKILLS:
		if not bool(card.get("legacy", false)):
			ids.append(String(card["id"]))
	return ids

## v3에서는 항상 false다(legacy 0장). 함수는 호출부 보호를 위해 남긴다.
static func is_legacy(id: String) -> bool:
	for card: Dictionary in SKILLS:
		if card["id"] == id:
			return bool(card.get("legacy", false))
	return false

static func by_id(id: String) -> Dictionary:
	if id == "basic":
		return BASIC.duplicate(true)
	for card: Dictionary in SKILLS:
		if card["id"] == id:
			return card.duplicate(true)
	for card: Dictionary in SPECIALS:
		if card["id"] == id:
			return card.duplicate(true)
	return {}

## 원소별 드래프트 풀. W6 편집 화면이 "이 칸에 맞는 카드" 필터로 쓴다.
static func by_element(element: String) -> Array[Dictionary]:
	var result: Array[Dictionary] = []
	for card: Dictionary in draft_pool():
		if String(card.get("element", "")) == element:
			result.append(card)
	return result

## 형태별 드래프트 풀. 삼각 배열(1·3·5칸 같은 형태) 안내에 쓴다.
static func by_form(form: String) -> Array[Dictionary]:
	var result: Array[Dictionary] = []
	for card: Dictionary in draft_pool():
		if String(card.get("form", "")) == form:
			result.append(card)
	return result

## 사거리 등급(§5.1). "melee"(<200) / "mid"(200~260) / "ranged"(≥260).
## 근접+원거리 혼합 단일 풀이라는 요구(부록 A-1 ⑥)가 지켜지는지 여기 한 곳에서 판정한다.
static func range_class(card: Dictionary) -> String:
	var r := float(card.get("range", 0.0))
	if r >= RANGED_RANGE_MIN:
		return "ranged"
	if r < MELEE_RANGE_MAX:
		return "melee"
	return "mid"

## 원소 → 드래프트 장수. 7원소 전부 CARDS_PER_ELEMENT(4)여야 한다.
static func element_counts() -> Dictionary:
	var counts: Dictionary = {}
	for element: String in ELEMENTS:
		counts[element] = 0
	for card: Dictionary in SKILLS:
		if bool(card.get("legacy", false)):
			continue
		var element := String(card.get("element", ""))
		if counts.has(element):
			counts[element] = int(counts[element]) + 1
	return counts

## 원소 → 형태 장수. 형태당 ≥5(삼각 배열이 5형태 전부에서 가능)를 여기서 센다.
static func form_counts() -> Dictionary:
	var counts: Dictionary = {}
	for form: String in FORMS:
		counts[form] = 0
	for card: Dictionary in SKILLS:
		if bool(card.get("legacy", false)):
			continue
		var form := String(card.get("form", ""))
		if counts.has(form):
			counts[form] = int(counts[form]) + 1
	return counts

## 원소 → {"melee":n, "mid":n, "ranged":n}. 근접/원거리 커버리지 단언의 단일 지점.
static func element_range_profile() -> Dictionary:
	var profile: Dictionary = {}
	for element: String in ELEMENTS:
		profile[element] = {"melee": 0, "mid": 0, "ranged": 0}
	for card: Dictionary in SKILLS:
		if bool(card.get("legacy", false)):
			continue
		var element := String(card.get("element", ""))
		if not profile.has(element):
			continue
		var bucket: Dictionary = profile[element]
		var key := range_class(card)
		bucket[key] = int(bucket[key]) + 1
	return profile

static func instance(id: String, rank: int = 1) -> Dictionary:
	# 저장되는 랭크 자체는 v1 그대로 1~5를 허용한다. v1 융합 경제(R5 2장 → 전직)와
	# 세이브가 아직 그 범위를 쓰기 때문이다. **수치 성장만** MAX_RANK에서 멈춘다(ranked()).
	return {"kind":"skill", "id":id, "rank":clampi(rank, 1, 5)}

static func basic_instance() -> Dictionary:
	return {"kind":"skill", "id":"basic", "rank":1, "default":true}

# -----------------------------------------------------------------------------
# 태그 표기 (§4.2)
# -----------------------------------------------------------------------------

static func element_name(element: String) -> String:
	match element:
		"fire": return "화(火)"
		"ice": return "빙(氷)"
		"thunder": return "뇌(雷)"
		"poison": return "독(毒)"
		"oil": return "유(油)"
		"strike": return "타(打)"
		"psi": return "초(超)"
		_: return "무속성"

## 원소의 역할 한 줄(§4.2). 카드 툴팁·온보딩·편집 화면 안내가 쓴다.
static func element_role(element: String) -> String:
	match element:
		"fire": return "짧고 굵은 도트 · 모든 폭발의 점화원"
		"ice": return "감속 · 전도의 매개"
		"thunder": return "전이 · 광역화"
		"poison": return "축적 → 터뜨리는 도트"
		"oil": return "피해 0 · 화염 증폭기"
		"strike": return "비원소 · 상태를 터뜨린다"
		"psi": return "비원소 · 상태를 환금한다"
		_: return "태그 없음"

## 상태를 **만드는** 원소인가(생산자). false면 거둬들이는 비원소다(§4.4 설계 의도 1).
static func element_is_producer(element: String) -> bool:
	return element in ["fire", "ice", "thunder", "poison", "oil"]

static func form_name(form: String) -> String:
	match form:
		"slash": return "참격"
		"pierce": return "관통"
		"wave": return "파동"
		"trap": return "설치"
		"guard": return "수호"
		_: return "무형"

## "화(炎) · 참격" 한 줄. 카드 툴팁·레일 배지가 그대로 쓴다.
static func tag_label(card: Dictionary) -> String:
	var element := String(card.get("element", ""))
	var form := String(card.get("form", ""))
	if element == "" and form == "":
		return ""
	return "%s · %s" % [element_name(element), form_name(form)]

static func kind_name(kind: String) -> String:
	match kind:
		"melee": return "근접 검격"
		"dash": return "돌진 검격"
		"projectile": return "원거리 검기"
		"chain": return "연쇄 공격"
		"area": return "주변 범위"
		"ground": return "설치 범위"
		"orbit": return "회전 지속"
		"shield": return "방어"
		_: return "특수 기술"

static func combat_tags(card: Dictionary) -> String:
	var tags: Array[String] = []
	var tag := tag_label(card)
	if tag != "":
		tags.append(tag)
	tags.append(kind_name(String(card.get("kind", ""))))
	var hits := maxi(1, int(card.get("hits", 1)))
	if hits > 1:
		tags.append("%d회 타격" % hits)
	if int(card.get("projectiles", 1)) > 1:
		tags.append("%d발" % int(card.get("projectiles", 1)))
	if int(card.get("shield", 0)) > 0:
		tags.append("수호막 +%d" % int(card.get("shield", 0)))
	if float(card.get("lifesteal", 0.0)) > 0.0:
		tags.append("흡혈")
	if float(card.get("slow", 0.0)) > 0.0:
		tags.append("둔화")
	if float(card.get("pull", 0.0)) > 0.0:
		tags.append("끌어당김")
	var reaction := knockback_profile(card)
	if float(reaction.get("force", 0.0)) >= 250.0:
		tags.append("강한 밀쳐내기")
	elif float(reaction.get("force", 0.0)) > 0.0:
		tags.append("밀쳐내기")
	elif float(reaction.get("stun", 0.0)) > 0.0:
		tags.append("경직")
	if bool(card.get("random_impacts", false)):
		tags.append("다중 낙하")
	return " · ".join(tags)

static func knockback_profile(card: Dictionary) -> Dictionary:
	# 수치는 이동 거리 자체가 아니라 초당 반동 속도입니다. 강한 기술일수록 더 멀고 오래 밀립니다.
	var kind := String(card.get("kind", "melee"))
	var force := 0.0
	var stun := 0.0
	match kind:
		"melee": force = 185.0; stun = 0.09
		"dash": force = 305.0; stun = 0.12
		"projectile": force = 118.0; stun = 0.055
		"chain": force = 72.0; stun = 0.045
		"area": force = 158.0; stun = 0.085
		"ground": force = 105.0; stun = 0.065
		"orbit": force = 88.0; stun = 0.045
		"shield": force = 215.0 if float(card.get("damage", 0.0)) > 0.0 else 0.0; stun = 0.1 if force > 0.0 else 0.0
	if float(card.get("pull", 0.0)) > 0.0:
		force = 0.0
		stun = maxf(stun, 0.075)
	var authored_knockback := float(card.get("knockback", 0.0))
	if authored_knockback > 0.0:
		force = maxf(force, 145.0 + authored_knockback * 4.1)
		stun = maxf(stun, 0.12)
	if bool(card.get("heavy", false)):
		force *= 1.32
		stun *= 1.45
	return {"force":force, "stun":stun}

# -----------------------------------------------------------------------------
# 랭크 (§5.2 · §9.3)
# -----------------------------------------------------------------------------

## 랭크 r의 계수 3종. 단일 진실 원천 — UI 미리보기도 이 함수를 쓸 것.
## r은 MAX_RANK(3)에서 포화한다. R1=1.00 / R2=1.55 / R3=2.10배 피해.
static func rank_formula(rank: int) -> Dictionary:
	var effective := clampi(rank, 1, MAX_RANK)
	var step := float(effective - 1)
	return {
		"rank": effective,
		"damage_mul": 1.0 + RANK_DAMAGE_STEP * step,
		"duration_mul": pow(RANK_DURATION_FALLOFF, step),
		"reload_mul": pow(RANK_RELOAD_FALLOFF, step),
		"range_mul": 1.0 + RANK_RANGE_STEP * step
	}

static func ranked(instance_data: Dictionary) -> Dictionary:
	var base := by_id(String(instance_data.get("id", "basic")))
	if base.is_empty():
		base = BASIC.duplicate(true)
	# 표시용 랭크는 저장된 값 그대로(R4·R5 융합 표기가 살아 있어야 한다).
	var stored_rank := clampi(int(instance_data.get("rank", 1)), 1, 5)
	base["rank"] = stored_rank
	base["kind"] = String(base.get("kind", "melee"))
	base["card_kind"] = "skill"
	base["element"] = String(base.get("element", ""))
	base["form"] = String(base.get("form", ""))
	if not bool(base.get("special", false)):
		# 수치 성장은 MAX_RANK에서 멈춘다(§5.2 "상한 R3").
		var f := rank_formula(stored_rank)
		base["effective_rank"] = int(f["rank"])
		base["damage"] = float(base.get("damage", 0.0)) * float(f["damage_mul"])
		base["duration"] = float(base.get("duration", 0.5)) * float(f["duration_mul"])
		base["reload"] = float(base.get("reload", 0.2)) * float(f["reload_mul"])
		base["range"] = float(base.get("range", 0.0)) * float(f["range_mul"])
	else:
		base["effective_rank"] = 1
	return base

# -----------------------------------------------------------------------------
# 드래프트 · 평가
# -----------------------------------------------------------------------------

static func random_two(rng: RandomNumberGenerator) -> Array[Dictionary]:
	# v3 풀(28종 = 7원소 × 4장) 전량에서 뽑는다.
	var pool := draft_pool()
	for index in range(pool.size() - 1, 0, -1):
		var other := rng.randi_range(0, index)
		var temporary: Dictionary = pool[index]
		pool[index] = pool[other]
		pool[other] = temporary
	return [pool[0], pool[1]]

static func expected_power(instance_data: Dictionary) -> float:
	var card := ranked(instance_data)
	var damage := float(card.get("damage", 0.0))
	var hits := maxi(1, int(card.get("hits", 1)))
	var projectiles := maxi(1, int(card.get("projectiles", 1)))
	var duration := maxf(0.12, float(card.get("duration", 0.5)))
	var utility := float(card.get("shield", 0)) * 0.7 + float(card.get("range", 0.0)) / 500.0
	return damage * hits * projectiles / duration + utility

static func debt_module(id: String) -> String:
	var card := by_id(id)
	var kind := String(card.get("kind", "melee"))
	match kind:
		"shield": return "firewall"
		"projectile", "chain": return "targeting"
		"ground", "area": return "overclock"
		"orbit": return "cache"
		"dash": return "targeting"
		_:
			if id in ["blood_pact", "pain_compiler", "immortal_frenzy"]:
				return "hotfix"
			if id in ["recursion", "infinite_recursion"]:
				return "recursion"
			return "overclock"
